<h1>{{ title }}</h1>
<ul>
    {{#each users}}
        <li>
                {{ number }} / {{ name }} / {{ major }} / {{ year }} / {{ enter }} / {{ mail }}
            </a>
        </li>
    {{/each}}
</ul>

